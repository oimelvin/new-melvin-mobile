import styled from 'styled-components/native'

import { ButtonOpacity } from '@styles/global.style'

export const IconButtonContainer = styled(ButtonOpacity)`
  justify-content: center;
  align-items: center;
  padding: 5px;
  margin: 5px;
`
