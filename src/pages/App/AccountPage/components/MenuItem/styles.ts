import styled from 'styled-components/native'

import { ButtonOpacity } from '@styles/global.style'

export const MenuItemContainer = styled(ButtonOpacity)`
	padding: 15px;
	flex-direction: row;
	align-items: center;
`
