import colors from '@styles/colors.style'
import styled from 'styled-components/native'

export const AcoesOrdemServicoPageContainer = styled.View`
	flex: 1;
	padding: 16px;
	background-color: ${() => colors.white};
`
