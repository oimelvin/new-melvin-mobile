import styled from 'styled-components/native'

import colors from '@styles/colors.style'

export const PlanejamentoOrdemServicoPageContainer = styled.View`
	flex: 1;
	padding: 16px;
	background-color: ${() => colors.white};
`
